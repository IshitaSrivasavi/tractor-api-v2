# Dockerized API

A simple Node.js API containerized with Docker.

## Features
- Hello World endpoint
- Health check endpoint
- Docker containerization

## How to Run

1. Build the Docker image:
   ```bash
   docker build -t my-docker-api .